# -*- Autoconf -*-

AT_SETUP([quiet turns off tickers])

MTN_SETUP
NETSYNC_SETUP

ADD_FILE(input.txt, [version 0 of the file
])
COMMIT(testbranch)

NETSYNC_SERVE_START(testbranch)

AT_CHECK(MTN --db=test2.db --keydir=keys2 --rcfile=netsync.lua pull --quiet NETSYNC_ADDRESS testbranch, [], [], [])

NETSYNC_SERVE_STOP

AT_CLEANUP
